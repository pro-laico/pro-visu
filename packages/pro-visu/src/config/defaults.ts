/** The config base name c12 discovers: pro-visu.config.*, .pro-visurc. */
export const CONFIG_NAME = "pro-visu";

/** The folder that holds the config file + its nested modules + output — `<repo>/pro-visu/`. */
export const CONFIG_DIR = "pro-visu";

/** Output directory (schema default + init/list fallbacks), relative to the config dir → `pro-visu/output/`. */
export const DEFAULT_OUTDIR = "output";
export const DEFAULT_CONCURRENCY = 1;
