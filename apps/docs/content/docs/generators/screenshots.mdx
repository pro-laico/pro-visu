---
title: screenshots
description: Responsive full-page and element captures, one set per viewport.
---

`screenshots` captures still images of a page at one or more viewports, optionally plus
specific elements. Output is png or jpeg.

```ts
{
  name: "home-shots",
  url: "https://your-site.com",
  generator: "screenshots",
  options: {
    viewports: [
      { name: "desktop", width: 1440, height: 900 },
      { name: "mobile", width: 390, height: 844 },
    ],
    elements: [{ selector: "header", name: "nav" }],
  },
}
```

A full-page desktop capture — the whole scrollable page, far taller than the 900px viewport
(scroll the panel):

<ImageExample
  tall
  src="/examples/docs-shot-desktop.jpg"
  width={2880}
  height={10090}
  alt="A full-page desktop screenshot of the VESPER storefront, captured well beyond the viewport height."
  caption="fullPage: true — Playwright resizes to the document height, so the whole page is in one shot."
  code={`{
  name: "home-desktop",
  generator: "screenshots",
  options: {
    viewports: [
      {
        name: "desktop",
        width: 1440,
        height: 900,
      },
    ],
    fullPage: true,
  },
}`}
/>

And the same site on a phone, just the viewport — as a visitor first sees it:

<ImageExample
  src="/examples/docs-shot-mobile.png"
  width={780}
  height={1688}
  alt="A mobile viewport screenshot of the VESPER storefront at 390×844."
  caption="fullPage: false on a 390×844 phone — the above-the-fold view, not the whole page."
  code={`{
  name: "home-mobile",
  generator: "screenshots",
  options: {
    viewports: [
      {
        name: "mobile",
        width: 390,
        height: 844,
      },
    ],
    fullPage: false,
  },
}`}
/>

## Options

| Option | Type | Default | Meaning |
|---|---|---|---|
| `viewports` | `{ name, width, height?, deviceScaleFactor? }[]` | desktop 1440×900 + mobile 390×844 | Viewports to capture at. `height` defaults to `900`; `deviceScaleFactor` is positive, max `4`. |
| `fullPage` | `boolean` | `true` | Capture the entire scrollable page vs. just the viewport. |
| `format` | `"png" \| "jpeg"` | `"png"` | Output format. |
| `quality` | `number` (1–100) | — | jpeg only. Rejected with `format: "png"`. |
| `deviceScaleFactor` | `number` (>0, ≤4) | `2` | Render scale; a viewport can override it. |
| `waitUntil` | `"load" \| "domcontentloaded" \| "networkidle" \| "commit"` | `"networkidle"` | Navigation wait condition. |
| `waitForSelector` | `string` | — | Wait for this selector (state `visible`) before capturing. |
| `elements` | `{ selector, name }[]` | `[]` | Element captures taken at every viewport. |
| `omitBackground` | `boolean` | `false` | png only — capture with a transparent background. |
| `settleMs` | `number` | `0` | Extra settle time before capturing. Floored at 600ms — see [Page warm-up](#page-warm-up). |

> `height` is ignored for `fullPage` shots (Playwright resizes to the page height); it only
> affects viewport and element captures.

Each viewport produces a page shot (unless you scope to elements), and each `elements` entry
produces one capture per viewport, named `<asset>-<viewport>-<element>`.

## Page warm-up

Before each shot the page is driven through a scroll/warm pass: it scrolls down through the full
document to trigger lazy-loaded and intersection-mounted content, lets web fonts finish loading,
and waits for images to decode, then returns to the top. Without this, `fullPage` shots tend to
capture blank or placeholder regions and fallback fonts further down the page.

The warm pass ends with a settle wait of `max(settleMs, 600)` milliseconds. Raise `settleMs`
above 600 for pages with slow animations or deferred network content the warm pass alone
doesn't catch.

## Element-capture resilience

Element captures never abort the run. If an `elements` selector matches zero nodes, or matches a
node that can't be shot (e.g. a present-but-hidden `display:none` element), that capture is
logged with a warning and skipped — the page shot and remaining elements/viewports still run.

## Rendering notes

- Viewports render in parallel (cap of 3 at a time), each in its own isolated browser context;
  element shots stay sequential within a viewport. Filenames and manifest ids stay in input
  order regardless.
- A very large `fullPage` shot (>16000px on either axis) emits a warning, since Chromium caps
  screenshots near ~32767px and a high `deviceScaleFactor` can push a tall page past that — the
  result may be clipped or slow. Lower the scale if you hit this.

## Capturing the UI in a particular state

`screenshots` captures the page as it loads — it's **stateless**, with no clicks or scripted steps.
To showcase the UI in a *state* (a menu open, a tab switched, a form filled), drive it with a
[`scroll-reel`](/docs/generators/scroll-reel) interaction instead and use that clip. Here, on a
phone viewport, a scripted tap opens the menu and holds it open:

<Example
  clip="docs-menu"
  caption="A phone-viewport scroll-reel standing in for a 'stateful screenshot' — the mobile menu open, captured."
  code={`{
  name: "menu-open",
  url: "https://your-site.com",
  generator: "scroll-reel",
  options: {
    width: 390,
    height: 844,
    cursor: { color: "#8c7355" },
    actions: [
      {
        do: "click",
        selector: "#menu-button",
        holdMs: 2600,
      },
    ],
  },
}`}
/>
