---
title: scroll-reel
description: Deterministic frame-stepped scroll recordings — single sweeps, choreographed section pans, and social formats.
---

`scroll-reel` is the workhorse generator, and it does one thing: record a page scrolling. Every
capture is **frame-stepped** — it drives a virtual clock, screenshots each frame, and pipes them
to ffmpeg — so output is frame-accurate, crisp (supersampled by `deviceScaleFactor`), parallelised
across `workers`, and **byte-identical run-to-run**.

```ts
{ name: "home", url: "https://your-site.com", generator: "scroll-reel" }
```

Here it *actually scrolls*: `autoSections` detects the page's sections and paces a pan/hold
down the whole page within a fixed time budget — hands-off, no choreography to author:

<Example
  clip="docs-scroll"
  caption="autoSections walks the home page top to bottom, holding briefly on each detected section."
  code={`{
  name: "home",
  url: "https://your-site.com",
  generator: "scroll-reel",
  options: {
    autoSections: {
      durationMs: 9000,
    },
  },
}`}
/>

And a seamless loop: `loop: "boomerang"` plays the capture forward then back, so the first and
last frames match:

<Example
  clip="docs-home"
  caption="A boomerang loop over an auto-sections pan — supersampled at deviceScaleFactor 3 for a crisp hero clip."
  code={`{
  name: "docs-home",
  generator: "scroll-reel",
  options: {
    deviceScaleFactor: 3,
    loop: "boomerang",
    autoSections: {
      durationMs: 22000,
      holdMs: 1600,
    },
  },
}`}
/>

> Want a scripted click-through with a visible cursor, a clip cropped to one component, or a
> **realtime** recording of the live page (time-based hero animation, autoplay video)? That's the
> [`interaction`](/docs/generators/interaction) generator. Site cleanup (hide the cookie banner,
> block trackers, freeze the clock) lives in
> [`settings.capture`](/docs/configuration/settings#capture) and applies to every URL capture.

## Core sizing

export const coreSizingOptions = [
  { name: 'width / height', description: 'Viewport size in CSS px. Default 1280 × 800.' },
  { name: 'fps', description: 'Output frames per second. Default 30 (max 120).' },
  { name: 'durationMs', description: 'Clip length in ms (when not using choreography/autoSections). Default 6000.' },
  { name: 'deviceScaleFactor', description: 'Render scale — higher captures crisper, then downscales into the video. Default 2 (max 4).' },
  { name: 'easing', description: 'Scroll easing: "linear", "ease-in", "ease-out", "ease-in-out" (default), "ease-out-strong", "ease-in-out-strong".' },
  { name: 'crf', description: 'x264 quality, 0–51 (lower = better/larger). Default 18.' },
  { name: 'fileName', description: 'Output filename. Defaults to <slug(asset name)>.mp4.' },
]

<ConfigOptions items={coreSizingOptions} />

## Timing & navigation

export const timingOptions = [
  { name: 'startDelayMs', description: 'Dwell at the top before scrolling (ms). Default 500.' },
  { name: 'endDwellMs', description: 'Dwell at the bottom after scrolling (ms). Default 800.' },
  { name: 'waitUntil', description: 'Navigation wait: "load", "domcontentloaded", "networkidle" (default), or "commit".' },
  { name: 'waitForSelector', description: 'Optional CSS selector to wait for (visible) before recording — e.g. a hero section.' },
]

<ConfigOptions items={timingOptions} />

## Frame rendering

How frames are rendered — not to be confused with [`settings.capture`](/docs/configuration/settings#capture),
the site cleanup applied to every URL capture.

export const frameRenderingOptions = [
  { name: 'workers', description: 'Parallel render contexts (auto-picks from cores + free memory; all captures share a run-wide context budget).' },
  { name: 'frameFormat', description: 'Intermediate frame format: "jpeg" (default) or "png" (lossless).' },
  { name: 'settlePerFrame', description: 'Wait for fonts + in-view images before each frame. Default on (off in --draft).' },
  { name: 'settleMaxMs', description: 'Per-frame settling cap in ms. Default 250.' },
]

<ConfigOptions items={frameRenderingOptions} />

## Motion

```ts
options: {
  choreography: [
    { to: "#hero", holdMs: 1200 },
    { to: "#features", holdMs: 1500 },
    { to: "100%", durationMs: 1000 },
  ],
  loop: "boomerang",             // play forward then back → seamless loop
}
```

- **`choreography: [{ to, durationMs?, holdMs?, easing? }]`** — replaces the single sweep with
  an authored sequence. `to` is a `0..1` number, an `"NN%"` string, or a CSS selector to bring
  into view. Per step: `durationMs` default `1200`, `holdMs` default `800`. Clip length becomes
  `startDelayMs` + Σ(step travel + hold) + `endDwellMs`.
- **`autoSections: true | { minHeightFraction?, selector?, holdMs?, durationMs?, maxSections?, constantVelocity? }`**
  — auto-detect the page's sections and pan/hold through them within a fixed `durationMs` budget.
  Defaults: `minHeightFraction` `0.5` (max `2`), `holdMs` `700`, `durationMs` `12000`,
  `maxSections` `8`, `constantVelocity` `true`. Ignored if `choreography` is set.
- **`loop: "none" | "boomerang"`** — boomerang plays forward then back for a seamless loop.
  Default `"none"`.

## Variants — one config, many assets

```ts
options: {
  colorScheme: "both",                              // → <name>-light and <name>-dark
  viewports: [
    { name: "desktop", width: 1440, height: 900 },
    { name: "mobile", width: 390, height: 844, deviceScaleFactor: 3 },
  ],                                                 // → <name>-desktop, <name>-mobile
}
```

- `colorScheme: "light" | "dark" | "both"` (with `themeClass` to toggle a CSS-class theme).
- `viewports: [{ name, width, height, deviceScaleFactor? }]`.

The viewport × colour-scheme matrix is emitted as separate assets (`<name>-<suffix>`).

## Output formats & framing

- **`aspect: "16:9" | "9:16" | "1:1" | { width, height }`** with `fit: "cover" | "contain"`
  (default `"cover"`) and `padColor` (default `#0b0b0f`, used by `"contain"`) — reframe for
  social (e.g. `9:16` reels).
- **`outputs: ("mp4" | "gif" | "webp" | "poster")[]`** (default `["mp4"]`) — each becomes its
  own asset; `gifFps` tunes the GIF/WebP frame rate (default `min(fps, 15)`, max `50`).

A vertical cut of the same page — `aspect: "9:16"` reframes the capture for a Reel or Story:

<Example
  clip="docs-vertical"
  portrait
  caption="The same scroll, reframed vertical for social. Add outputs for gif/poster to cover organic formats too."
  code={`{
  name: "home-vertical",
  url: "https://your-site.com",
  generator: "scroll-reel",
  options: {
    width: 430,
    height: 932,
    durationMs: 5000,
    aspect: "9:16",
  },
}`}
/>

> Every option also has hover docs in `pro-visu.config.ts` — the authoring types are generated
> from the validation schema, so the editor always matches what the tool accepts.
