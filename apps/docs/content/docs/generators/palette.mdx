---
title: palette
description: A still colour-palette image with labelled, auto-contrasting swatches.
---

`palette` renders a still image of colour swatches, each labelled with the fields you place in
its corners (name / hex / rgb / oklch / hsl / cmyk) with auto-contrasting text. Only `colors`
is required. No `url`. Output is png.

```ts
{
  name: "brand-colors",
  generator: "palette",
  options: {
    colors: [
      { name: "Ink", hex: "#1a1714" },
      { name: "Camel", hex: "#b49a77" },
      { name: "Paper", hex: "#f6f3ed" },
    ],
    layout: "columns",
    uppercase: true,
  },
}
```

<ImageExample
  src="/examples/docs-palette.png"
  width={2800}
  height={3500}
  alt="The VESPER brand palette rendered as five labelled swatches with name, hex, rgb, and oklch values."
  caption="The default rows layout — each swatch labelled with auto-contrasting text."
  code={`{
  name: "colors",
  generator: "palette",
  options: {
    colors: [
      { name: "Ink", hex: "#1a1714" },
      { name: "Paper", hex: "#f6f3ed" },
      { name: "Camel", hex: "#b49a77" },
      { name: "Loden", hex: "#5c5e4c" },
      { name: "Cognac", hex: "#8a5a3c" },
    ],
  },
}`}
/>

## Options

export const options = [
  { name: 'colors', type: '{ name, hex }[]', description: 'The colours to show (≥1). hex accepts "#rgb" or "#rrggbb".', required: true },
  { name: 'layout', type: '"rows" | "columns" | "grid"', default: '"rows"', description: 'Full-width bands, full-height columns, or an N-wide grid.' },
  { name: 'gridColumns', type: 'number (1–12)', default: '3', description: 'Columns when layout: "grid".' },
  { name: 'width / height', type: 'number', default: '1400 / 1750', description: 'Output size (default is portrait 4:5).' },
  { name: 'deviceScaleFactor', type: 'number (≤4)', default: '2', description: 'Render scale.' },
  { name: 'background', type: 'string', default: '"#ffffff"', description: 'Page background, shown only in the gaps.' },
  { name: 'gap', type: 'number', default: '0', description: 'Gap between swatches (px).' },
  { name: 'cornerRadius', type: 'number', default: '0', description: 'Swatch corner radius (px).' },
  { name: 'topLeft / topRight / bottomLeft / bottomRight', type: 'FieldId[]', default: 'TL ["name","hex"], TR ["rgb","oklch"]', description: 'Fields shown (stacked) in each corner.' },
  { name: 'uppercase', type: 'boolean', default: 'false', description: 'Uppercase the colour names.' },
  { name: 'rgbStyle', type: '"labeled" | "css" | "plain"', default: '"labeled"', description: 'RGB string style.' },
  { name: 'oklchStyle', type: '"css" | "labeled"', default: '"css"', description: 'OKLCH string style.' },
  { name: 'fontFile', type: 'string', default: 'system sans fallback', description: 'Custom font (.woff2 / .woff / .ttf / .otf) embedded into the render. Optional — omit to fall back to a system bold sans.' },
  { name: 'fontSize', type: 'number', default: 'derived', description: 'Label font size in px.' },
  { name: 'fontWeight', type: 'number (1–1000)', default: '700', description: 'Label font weight.' },
  { name: 'textLight / textDark', type: 'string', default: '#ffffff / #141414', description: 'Text colours picked by contrast against each swatch.' },
  { name: 'contrastThreshold', type: 'number (0–1)', default: '0.5', description: 'Luminance above which dark text is used.' },
  { name: 'padding', type: 'number', default: 'derived', description: 'Inset of labels from swatch edges (px).' },
  { name: 'fileName', type: 'string', default: '<name>.png', description: 'Output filename.' },
]

<ConfigOptions items={options} />

The available corner field ids are `name`, `hex`, `rgb`, `oklch`, `hsl`, and `cmyk`.

Place specific fields in each corner (each corner stacks its list top→bottom), and pick a layout:

```ts
{
  name: "brand-colors",
  generator: "palette",
  options: {
    colors: [
      { name: "Ink", hex: "#1a1714" },
      { name: "Camel", hex: "#b49a77" },
      { name: "Paper", hex: "#f6f3ed" },
      { name: "Loden", hex: "#5c5e4c" },
    ],
    layout: "grid",
    gridColumns: 2,
    topLeft: ["name"],
    topRight: ["hex"],
    bottomLeft: ["rgb"],
    bottomRight: ["oklch"],
    rgbStyle: "css",                     // rgb(26, 23, 20)
    fontFile: "public/fonts/Inter.woff2", // optional — embeds your brand font
    uppercase: true,
  },
}
```

## Field formats

How each field renders:

| Field | Style | Example |
|---|---|---|
| `hex` | — | `#FF8000` (always uppercased) |
| `rgb` | `"labeled"` | `R:255,G:128,B:0` |
| `rgb` | `"css"` | `rgb(255, 128, 0)` |
| `rgb` | `"plain"` | `255 128 0` |
| `oklch` | `"css"` | `oklch(88% 0.012 250)` |
| `oklch` | `"labeled"` | `L:88% C:0.012 H:250` |
| `hsl` | — | `H:210,S:14,L:88` (s/l are percentages) |
| `cmyk` | — | `C:0,M:50,Y:100,K:0` (percentages) |

For a moving version, see [`palette-reel`](/docs/generators/palette-reel).
