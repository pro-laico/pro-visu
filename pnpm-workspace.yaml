packages:
  - "packages/*"
  - "apps/*"

# pnpm reads the build-script allowlist from here. These packages run install/build
# scripts to fetch their native binaries.
allowBuilds:
  esbuild: true
  ffmpeg-static: true
  sharp: true
