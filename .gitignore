node_modules/
dist/
.next/
.turbo/
out/
coverage/
next-env.d.ts
*.tsbuildinfo
*.log
.DS_Store
.idea/
.vscode/

# generated showcase output (testing app) — the pro-visu/ folder itself holds the committed
# config; only its output/ subdir (default convention) and the served public/pro-visu/ are ignored.
apps/*/pro-visu/output/
apps/*/public/pro-visu/

# fumadocs generated source
**/.source/

# local scratch / e2e sandbox
/.scratch/
/test-site/

# personal / machine-specific
.claude/settings.local.json
*.code-workspace
.mcp.json

# env files with secrets (e.g. Mux tokens for apps/docs next-video)
.env
.env*.local
